require './lib/bitcoin'
require 'irb'

include Bitcoin::Util
include Bitcoin::Storage

network = (ARGV.shift || :testnet).to_sym

Bitcoin::network = network

ActiveRecord::Base.establish_connection YAML::load(File.read("db/database.yml"))[network.to_s]

IRB.start
